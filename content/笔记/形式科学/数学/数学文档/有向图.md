##### 有向图
- 有向图
	- **有向图**是有向边构成的[[图]] $G = (V, E)$, 有向边表示从一个顶点指向另一个顶点的单向连接, 即有序对 $(u,v)$, 边的集合为 $E \subseteq \{(u, v) \mid u, v \in V\}$. [[有向图的度]]满足度数和等式
		- $G = (V, E)$, $E \subseteq \{(u, v) \mid u, v \in V\}$

