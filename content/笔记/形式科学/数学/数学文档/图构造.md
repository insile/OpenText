##### 图构造
- 图构造
	- 图构造指通过对已有[[图]]进行变换, 扩展或组合, 生成新的图结构
		- 从图 $G = (V, E)$ 中选取一部分顶点及其关联的边, 构成的新图称为子图. 顶点诱导子图指选择顶点集的子集 $V' \subseteq V$, 保留其内部的边, 边诱导子图指选择边集的子集 $E' \subseteq E$, 保留其关联的顶点
		- 图的变换有边收缩, 指将一条边的两个端点合并为一个顶点, 删除该边, 同时保留其他边. 边删除, 指从图中移除一条边, 但保留顶点. 顶点删除, 指从图中移除一个顶点及其相关的所有边
