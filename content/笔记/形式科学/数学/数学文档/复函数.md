##### 复函数
- 复函数
	- **复函数**是[[复数]]之间的[[映射]] $f:D\rightarrow\mathbb{C}$, 其中 $D\subseteq\mathbb{C}$. 如果只有一个值 $y$ 对应于每个值 $z$, 则 $f(z)$ 是单值函数, 如果有多个值 $y$ 对应于每个值 $z$, 则 $f(z)$ 是[[多值函数]], 常见有[[初等函数]]的复扩展, 而[[解析函数]]是复函数中最重要的一类, 可能存在一些行为异常的奇点
		- $f(z) = u(x, y) + i v(x, y)$, $z = x + iy \in \mathbb{C}$

