##### 相异代表系
- 相异代表系
	- **相异代表系**描述如何从多个[[集合]]中选择元素, 使得每个集合选择的元素都不重复. 给定一组集合 $S_1, S_2, \dots, S_n$, 一个相异代表系是一个集合 $\{x_1, x_2, \dots, x_n\}$, 其中 $x_i \in S_i$ 且 $x_i \neq x_j$​ 对于所有 $i \neq j$. 即选择的元素 $x_1, x_2, \dots, x_n$​ 是来自每个集合 $S_i$​ 的代表元素, 并且这些代表元素之间互不相同

