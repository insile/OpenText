##### 含参积分
- 含参积分
	- **含参积分**是指[[积分]]的被积函数中带有参数, 积分结果本身会随着参数的变化而变化. 把一个普通积分变成含参积分, 可以利用这个参数来控制或简化原积分. 形式上, 如果函数 $f(x, \alpha)$ 定义在区间 $[a, b]$ 上, 并且 $\alpha$ 是一个参数, 则含参积分定义为 $F(\alpha)$ 
		- $\displaystyle F(\alpha)= \int_a^b f(x, \alpha)\mathrm{d}x$

