##### 克罗内克函数
- 克罗内克函数
	- **克罗内克函数**是定义在离散变量上的[[映射]], 如果输入变量两者相等, 则其输出值为 $1$, 否则为 $0$, 一般定义在整数上即 $\delta_{ij}:\mathbb{Z}\times\mathbb{Z}\to\{1,0\}$. [[单位矩阵]] $I_n$​ 的元素可以用克罗内克函数表示 $(I_n)_{ij}=\delta_{ij}$, 通常用于表示元素级操作
		- $\delta_{ij} = \begin{cases} 1, &  i = j \\ 0, & i \neq j \end{cases}$

