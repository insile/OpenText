##### 子空间的并
- 子空间的并
	- **子空间的并**是两个[[向量子空间]] $U,W \subseteq V$ 的集合[[并集]] $U \cup W$, 即属于 $U$ 或 $W$ 的所有向量组成的子集, 通常不是[[向量空间]]
		- $U \cup W = \{ \mathbf{v} \in V \mid \mathbf{v} \in U \lor \mathbf{v} \in W \}$

