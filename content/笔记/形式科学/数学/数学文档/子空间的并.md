##### 子空间的并
- 子空间的并
	- **子空间的并**是 $V$ 两个[[子空间]] $U,W$ 的集合[[并集]] $U \cup W$, 即属于 $U$ 或 $W$ 的所有向量组成的子集, 子空间的并通常**不是向量空间**
		- $U \cup W = \{ \mathbf{v} \in V \mid \mathbf{v} \in U \lor \mathbf{v} \in W \}$

