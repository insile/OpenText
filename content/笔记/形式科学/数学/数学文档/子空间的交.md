##### 子空间的交
- 子空间的交
	- **子空间的交**是两个[[向量子空间]] $U,W \subseteq V$ 的集合[[交集]] $U \cap W$, 即同时属于 $U$ 和 $W$ 的所有向量组成的子集, 仍然是[[向量空间]]
		- $U \cap W = \{ \mathbf{v} \in V \mid \mathbf{v} \in U \land \mathbf{v} \in W \}$
		- $\dim(U \cap W) \leq \min(\dim(U), \dim(W))$
