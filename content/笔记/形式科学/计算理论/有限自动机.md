##### 有限自动机
- 有限自动机
	- **有限自动机** $\text{FA}$ 是一种用于描述和处理有限状态系统的[[自动机]], 它可以看作是一个简单的机器, 根据输入字符串的字符和当前状态决定下一步的行为, 有限自动机识别和处理[[正则语言]], 主要分为[[确定型有限自动机]]和[[非确定型有限自动机]], 唯一区别在于状态转移函数的返回值类型, 即返回单个确定状态和返回状态集合, 但是二者具有等价性. 设计有限自动机需要通过模拟自动机行为, 确定关键信息, 并将其转化为状态和转移规则

