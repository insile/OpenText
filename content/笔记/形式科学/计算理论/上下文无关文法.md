##### 上下文无关文法
- 上下文无关文法
	- **上下文无关文法**是[[上下文无关语言]]的[[形式语法]] $G = (V, \Sigma, P, S)$, 其中每条产生规则的左侧必须是单个非终结符, 这意味着无论这个非终结符出现在句子中的哪个位置, 它都可以独立地被规则替换, 而不依赖于它的上下文
		- $A \to \gamma$
			- $A$ 是单个非终结符
			- $\gamma$ 是由终结符和非终结符组成的字符串

