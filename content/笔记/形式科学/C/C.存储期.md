##### 存储期
- 存储期
	- 每个[[C.对象|对象]]都有称为**存储期**的属性, 它限制对象的[[C.生存期|生存期]]
		- 自动存储期, 进入对象所声明于其中的块作用域时分配其存储, 退出该块时销毁
		- 静态存储期, 生命周期贯穿程序的整个执行过程
		- 线程存储期, 在整个线程生命周期内有效
		- 动态存储期, 由 malloc, calloc 等动态分配的内存, 直到被释放时才会销毁

