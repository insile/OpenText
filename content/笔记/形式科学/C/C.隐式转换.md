##### 隐式转换
- 隐式转换
	- 隐式转换是指编译器在[[C.表达式|表达式]]中自动将一种[[C.类型|类型]]转换为另一种兼容的类型, 而不需要程序员明确指定
- 语法
	- 整数提升 如 `char` 转为 `int`
	- 算术转换 如 `int` 转为 `float`
	- 浮点数转换 如 `float` 转为 `double`
	- 指针转换 如 `void*` 转为其他类型的指针
	- 数组到指针转换
	- 函数到指针转换
