---
创建时间: 2026-02-14
更新时间: 2026-08-29
页面类型: 1
更新次数: 2
准确率: 100
完整度: 60
阅读状态: 0
---
##### 算子的行列式
- 算子的行列式
	- **算子的行列式**是通过[[多重线性型|交错多重线性型]]定义的[[线性算子]]的内蕴标量, 它衡量了算子对空间体积的缩放比例, 揭示了算子的[[可逆算子|可逆性]], 且具有[[向量空间的基|基]]无关性, 算子的行列式等于任意基下的矩阵的[[行列式]]. 设 $V$ 是数域 $\mathbb{F}$ 上的 $n$ 维向量空间, $T: V \to V$ 为线性算子. 算子 $T$ 的行列式 $\det(T)$ 可以通过 $V$ 上的非零交错多重线性型进行不依赖于坐标系的内蕴定义. 设 $f: V^n \to \mathbb{F}$ 是 $V$ 上的一个非零交错 $n$-重线性型, 算子 $T$ 在 $f$ 上诱导了一个新的交错多重线性型 $D_T: V^n \to \mathbb{F}$, 称为体积函数, 由于 $n$ 维空间上交错 $n$-重线性型构成的向量空间维数为 $1$, 诱导形式 $D_T$ 必为 $f$ 的标量倍, 这个唯一的比例因子即定义为 $\det(T)$, 并且对于[[恒等算子]] $T=I$ 满足归一化要求 $\det(I) = 1$, 即体积缩放不变. 在代数闭域上, 算子的行列式等于其所有[[特征值和特征向量|特征值]]按[[特征值的重数|代数重数]]的乘积 $\det(A)=\prod^{n}_{i=1}\lambda_i$
		- 诱导形式, $D_T(\mathbf{v}_1, \mathbf{v}_2, \dots, \mathbf{v}_n) = f(T(\mathbf{v}_1), T(\mathbf{v}_2), \dots, T(\mathbf{v}_n))$
		- 比例因子, $D_T(\mathbf{v}_1, \mathbf{v}_2, \dots, \mathbf{v}_n) = \det(T) \cdot f(\mathbf{v}_1, \mathbf{v}_2, \dots, \mathbf{v}_n)$
		- 归一化, $D_I(\mathbf{v}_1 , \mathbf{v}_2 , \dots, \mathbf{v}_n ) = f(I(\mathbf{v}_1 ), I(\mathbf{v}_2 ), \dots, I(\mathbf{v}_n )) = 1 \cdot f(\mathbf{v}_1 , \mathbf{v}_2 , \dots, \mathbf{v}_n )$


>[!example]- 算子的行列式
> - 设线性算子 $T: V \to V$, 选择 $V$ 的一组基 $\{\mathbf{e}_1, \mathbf{e}_2, \dots, \mathbf{e}_n \}$. 则 $T$ 在该基下由矩阵 $A = [a_{ij}]$  表示, 并且有
> 	- $\displaystyle T(\mathbf{e}_j) = \sum_{i=1}^n a_{ij} \mathbf{e}_i$
> - 设标准交错多重线性型 $f$, 其中 $\text{sgn}(\sigma)$ 是置换符号, 它不仅是一个交错多重线性型, 更是标准的默认满足 $f(\mathbf{e}_1, \mathbf{e}_2, \dots, \mathbf{e}_n ) = 1$, 也就是将基的空间比例标准化为 $1$
> 	- $f(\mathbf{e}_{i_1}, \mathbf{e}_{i_2}, \dots, \mathbf{e}_{i_n}) = \begin{cases} \text{sgn}(\sigma), & \text{若 } (i_1, i_2, \dots, i_n) \text{ 是 } (1, 2, \dots, n) \text{ 的一个置换 } \sigma \\ 0, & \text{若有任意两个 } i_k \text{ 相同} \end{cases}$
> - 现在计算  $D_T(\mathbf{e}_1, \mathbf{e}_2, \dots, \mathbf{e}_n )$ 
> 	- $\displaystyle D_T(\mathbf{e}_1, \mathbf{e}_2, \dots, \mathbf{e}_n ) = f(T(\mathbf{e}_1), T(\mathbf{e}_2), \dots, T(\mathbf{e}_n ))$
> 	- $\displaystyle D_T(\mathbf{e}_1, \mathbf{e}_2, \dots, \mathbf{e}_n ) = f\left( \sum_{i_1} a_{i_1 1} \mathbf{e}_{i_1}, \sum_{i_2} a_{i_2 2} \mathbf{e}_{i_2}, \dots, \sum_{i_n} a_{i_n n} \mathbf{e}_{i_n} \right)$
> - 由于 $f$ 的多重线性性, 将其展开有
> 	- $\displaystyle D_T(\mathbf{e}_1, \mathbf{e}_2, \dots, \mathbf{e}_n ) = \sum_{i_1, i_2, \dots, i_n} a_{i_1 1} a_{i_2 2} \cdots a_{i_n n} f(\mathbf{e}_{i_1}, \mathbf{e}_{i_2}, \dots, \mathbf{e}_{i_n})$
> - 由于 $f$ 的交错性, 只有当 $(i_1, i_2, \dots, i_n)$ 是 $\{1, 2, \dots, n\}$ 的置换时  $f(\mathbf{e}_{i_1}, \mathbf{e}_{i_2}, \dots, \mathbf{e}_{i_n}) = \text{sgn}(\sigma)$, 否则为零, 因此
> 	- $\displaystyle D_T(\mathbf{e}_1, \mathbf{e}_2, \dots, \mathbf{e}_n ) = \sum_{\sigma \in S_n} \text{sgn}(\sigma) a_{\sigma(1) 1} a_{\sigma(2) 2} \cdots a_{\sigma(n) n}$
> - 所以这正是矩阵 $A$ 的行列式, 对矩阵的列向量进行置换
> 	- $\displaystyle \det(A) = \sum_{\sigma \in S_n} \text{sgn}(\sigma) a_{1 \sigma(1)} a_{2 \sigma(2)} \cdots a_{n \sigma(n)}$
> - 因此
> 	- $\det(T) = \det(A)$ 

