##### 空类型声明
- 空类型声明
	- **空类型声明**是使用空类型类型说明符[[C.声明|声明]]没有返回值[[C.函数|函数]]或指向未知类型数据的[[C.指针|指针]]
- 语法
	- `void myFunction(void);`
	- `void *ptr`
- 示例
```c
void print_message(void);  // 声明一个无返回值的函数
void *ptr;  // 声明一个 void 类型的指针，可以指向任何数据类型
```

