##### 赋值表达式
- 赋值表达式
	- **赋值表达式**是使用[[C.赋值运算符|赋值运算符]] `=` 将值赋给变量的表达式, 赋值表达式会对左操作数进行更新, 并返回整个赋值表达式的结果
- 示例
```c
#include <stdio.h>

int main() {
    int x = 0;
    x = 10; // 赋值 10 给 x
    printf("x = %d\n", x); // 输出 10
    return 0;
}

```
